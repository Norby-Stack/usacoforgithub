#include <bits/stdc++.h>
using namespace std;

int main() {
    int T,C;
    cin >> T >> C;
    vector<int> targets(T);
    for (int i=0;i<T;i++)
    {
        cin >> targets[i];
    }
    string instructions;
    cin >> instructions;
    vector<int> subtracttwo(instructions.size());
}